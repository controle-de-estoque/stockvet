spring:
  application:
    name: backend

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://localhost:5000/stockvet}
    username: ${DB_USERNAME:stockvet}
    password: ${DB_PASSWORD:stockvet}
    driver-class-name: org.postgresql.Driver
    
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 20000

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        show_sql: false
        format_sql: false
    open-in-view: false
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_ORIGIN:stockvet.contato@gmail.com}
    password: ${MAIL_PASSWORD:ufub msov exec rpqq}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${JWT_SECRET:hRg2nzthwKbldOyO6tMRfmP0KOGZo04SuggwrCP71gg}
  expiration: ${JWT_EXPIRATION:3600000}

server:
  port: ${PORT:8080}
  shutdown: graceful